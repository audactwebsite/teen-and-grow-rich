---
/**
 * The Napoleon Hill audit (docs/decisions.md D26).
 *
 * This is the site's credibility centrepiece, and it exists because the largest
 * unmitigated reputational risk in the project is that a book whose first promise is
 * "no fake case studies" is built on the most famous unverifiable case-study collection
 * in publishing. Publishing the audit ourselves is cheaper than being handed it.
 *
 * Two rules this page implements literally: the site never describes the book as
 * inheriting Hill's authority, only as remapping his ideas; and every number links to a
 * primary source or gets cut.
 *
 * The two source links carry rel="external" — they are links, not subresources, so the
 * no-external-hosts check still passes, and Base.astro's speculation rules exclude them
 * from prerendering.
 */
import Page from "../layouts/Page.astro";
import ChapterFurniture from "../components/ChapterFurniture.astro";
import { CONTACT_EMAIL } from "../lib/site";

/* D29: dated claims carry a verification date, and go stale visibly rather than
   quietly. A date that has drifted testifies that nobody is home. */
const VERIFIED_ON = "2026-07-30";
const verifiedOn = new Date(VERIFIED_ON);
const daysSince = Math.floor((Date.now() - verifiedOn.getTime()) / 86_400_000);
const stale = daysSince > 180;
const verifiedLabel = verifiedOn.toLocaleDateString("en-GB", {
  day: "numeric",
  month: "long",
  year: "numeric",
});

/** The record, in date order. Every row is checkable from the two sources below. */
const RECORD = [
  {
    when: "c. 1908",
    what:
      "The commission. Hill said Andrew Carnegie handed him the assignment that became his life's work: interview the men who had built America's fortunes and reduce what they did to a set of principles. David Nasaw, who wrote the standard biography of Carnegie and worked through his papers, found no evidence that Hill and Carnegie ever met.",
  },
  {
    when: "1908-1912",
    /* This row used to read "While he places the Carnegie commission, Hill was on the run
       over a lumber fraud in Alabama." — a subordinate clause with no object, in the only
       ungrammatical line on the page. The date moved into the key so the sentence can
       carry its own subject. */
    what:
      "In the same years he says he was working for Carnegie, Hill was on the run over a lumber fraud in Alabama.",
  },
  {
    when: "April 1912",
    what:
      "The trade magazine Motor World described Hill's Automobile College of Washington as a scam.",
  },
  {
    when: "June 1918",
    what:
      "Illinois issued warrants for Hill under its blue-sky laws — the laws that exist to stop people selling worthless shares.",
  },
  {
    when: "1919",
    what:
      "The Federal Trade Commission, the US government body that polices dishonest selling, brought charges against him.",
  },
  {
    when: "Throughout",
    what:
      "The interviews themselves. Beyond one brief encounter with Thomas Edison, there are no records of Hill interviewing the famous men whose lessons he reported.",
  },
];

/** Numbers this site refuses, and why. Both are canon in self-help and neither exists. */
const MYTHS = [
  {
    claim: "The 1953 Yale goal-setting study",
    story:
      "3% of Yale's class of 1953 wrote their goals down, and twenty years later that 3% was worth more than the other 97% combined.",
    why:
      "Yale has no record of such a study. A 1996 Fast Company investigation went looking for it and could not find anyone able to produce it, at Yale or anywhere else. It survives because it is useful, not because it happened.",
  },
  {
    claim: "The 65% / 95% accountability figures",
    story:
      "You are 65% more likely to reach a goal if you tell someone, and 95% more likely if you book a regular check-in with them.",
    why:
      "Nobody can say where these came from. There is no study, no sample size and no method behind them — just the numbers, passed from book to book. The idea underneath them — telling one person helps — is in this book as step four of Chapter 1's Play, Choose one witness, with no invented number attached.",
  },
];

/* The <title> below is the question a reader actually types, not our internal name for this
   page. A review pointed out that /hill answers "is Napoleon Hill's story true?" better than
   anything else on the web, and was titled so that nobody searching for that answer would
   ever find it. The H1 keeps the brand term; the title is the findable one.
   Non-negotiable 8 still holds: asking whether his story checks out implies no affiliation,
   endorsement or reproduction of any kind — it implies the opposite. */
---

<Page
  title="Is Napoleon Hill's story true? The checkable record, with dates"
  description="Napoleon Hill's principles are worth testing; his own stories were not checkable. The record, with dates and sources, and why this book uses labelled composites instead."
>
  {/* The H1 used to be "The Napoleon Hill audit" — a finance word, twice in the first two
      lines, never glossed anywhere in the rendered text. The plain-English version of the
      page existed only in the browser tab. An eyebrow is allowed to be a label, so the
      brand term stays there and the H1 is the question the reader already has.
      The subtitle was two abstractions: "remaps" is design vocabulary and "inherit his
      authority" is an idea a thirteen-year-old has to reverse-engineer. */}
  <ChapterFurniture
    eyebrow="The audit"
    title="Is Napoleon Hill's story true?"
    subtitle="This book rebuilds Napoleon Hill's ideas for you. It does not ask you to trust him. Here is the difference, with dates."
  />

  <div class="wrap">
    <div class="prose">
      <p class="lede">
        Hill's principles are worth testing. His biography is not. Historians and
        journalists who went looking for the meetings, the interviews and the credentials
        behind <i>Think and Grow Rich</i> did not find them — and they did find fraud
        charges. This page says so plainly, because a book that says it will never pass off
        an invented story as real cannot quietly do it.
      </p>
    </div>
  </div>

  <section class="wrap section" aria-labelledby="record">
    <h2 id="record">What the record shows</h2>
    <div class="rows">
      {
        RECORD.map((r) => (
          <div class="row">
            <p class="row__key">{r.when}</p>
            <p class="row__value">{r.what}</p>
          </div>
        ))
      }
    </div>

    <div class="prose sources">
      <p class="sources__label">Sources</p>
      <ul>
        <li>
          <a href="https://en.wikipedia.org/wiki/Napoleon_Hill" rel="external noreferrer">
            Napoleon Hill — Wikipedia
          </a>, which carries the dated citations for the warrants, the charges and the
          Motor World report.
        </li>
        <li>
          <a
            href="https://gizmodo.com/the-untold-story-of-napoleon-hill-the-greatest-self-he-1789385645"
            rel="external noreferrer"
          >
            The Untold Story of Napoleon Hill, the Greatest Self-Help Scammer of All Time
          </a> — Gizmodo's long account, which is where most of the primary documents were
          first put side by side.
        </li>
      </ul>
      <p class="sources__date">
        Checked on {verifiedLabel}.
        {
          stale && (
            <span class="sources__stale">
              We have not re-checked this since then — confirm it yourself before relying
              on it.
            </span>
          )
        }
      </p>
    </div>
  </section>

  <section class="wrap section" aria-labelledby="turn">
    <h2 id="turn">Then why is the book built on him?</h2>
    <div class="prose">
      <p>
        Because the principles and the man are two different things. Desire, decision,
        planning, persistence, a small honest team, paying attention to where your
        attention goes — none of that is magic, and you can test all of it yourself in
        ninety days. Those ideas hold up. You do not have to trust Hill to try them. You
        try them, and then you look at what you actually made.
      </p>
      <p>
        What does not hold up is how Hill proved things: stories about famous men that
        nobody can check, used as evidence. So this book does the opposite, and says so on
        printed page 2 rather than in a footnote:
      </p>
      <blockquote>
        <p>
          The stories in this edition are fictional composites. They are designed to make
          the principles easy to understand; they are not presented as verified case
          studies. Examples of income, audience growth, or opportunity are not promises or
          typical results.
        </p>
        <cite>Teen &amp; Grow Rich, Publishing Note, printed page 2</cite>
      </blockquote>
      <p>
        That is the whole trade. The stories in the book are labelled as invented, so
        nothing rests on them. Everything you are actually meant to believe —{" "}
        <a href="/proof">Ryan's race results, with the sources</a>, every number anywhere
        on this site — has a source next to it, or it is not here.
      </p>
    </div>
  </section>

  <section class="wrap section" aria-labelledby="myths">
    <h2 id="myths">Statistics you will not find on this site</h2>
    <div class="prose">
      <p>
        These two are quoted constantly in books like this one. Both are unsourced. We are
        naming them so you know we left them out on purpose, not because we missed them.
      </p>
    </div>

    <div class="rows">
      {
        MYTHS.map((m) => (
          <div class="row">
            <p class="row__key">{m.claim}</p>
            <div class="row__value">
              <p class="myth__claim">“{m.story}”</p>
              <p class="myth__why">{m.why}</p>
            </div>
          </div>
        ))
      }
    </div>

    {/* This section had no sources at all, on the page whose rule is that claims carry
        them — and the Sources block above scopes itself explicitly to the other section.
        The 65/95 figures get no link because there is nothing to link to, which is the
        row's own finding; the row now says that in those words rather than hedging it as
        "a training-industry body". */}
    <div class="prose sources">
      <p class="sources__label">Sources</p>
      <ul>
        <li>
          Lawrence Tabak,{" "}
          <a
            href="https://www.fastcompany.com/27953/if-your-goal-success-dont-consult-these-gurus"
            rel="external noreferrer"
          >
            If Your Goal Is Success, Don't Consult These Gurus
          </a>, Fast Company, 1996 — the investigation that went looking for the Yale study,
          asked Yale, and found no record of it or of anyone who remembered it.
        </li>
      </ul>
    </div>

    <div class="prose rule-note">
      <p>
        <strong>So here is the rule:</strong> every number on this site links to a primary
        source, or it gets cut. Found one that does not? Email{" "}
        <a href={`mailto:${CONTACT_EMAIL}`}>{CONTACT_EMAIL}</a> — an adult reads it, and
        telling us is a favour.
      </p>
    </div>
  </section>
</Page>

<style>
  /* Container contract, matched to Page.astro and ChapterFurniture. */
  .wrap {
    max-inline-size: 72rem;
    margin-inline: auto;
    padding-inline: clamp(1.25rem, 5vw, 3rem);
  }

  .prose {
    max-inline-size: var(--container-measure);
  }

  .prose p + p,
  .prose ul,
  .prose blockquote {
    margin-block-start: 0.9em;
  }

  .lede {
    margin-block-start: 1.75rem;
    font-size: var(--text-subtitle);
    line-height: var(--text-subtitle--line-height);
    color: var(--text-strong);
  }

  .section {
    margin-block-start: 3rem;
  }

  .section > h2 {
    font-size: var(--text-h2);
    line-height: var(--text-h2--line-height);
    color: var(--text-section);
    max-inline-size: var(--container-measure);
  }

  /* The page rhythm: full-width hairline data rows, then a 65ch prose column. */
  .rows {
    margin-block-start: 1.5rem;
    border-block-start: 1px solid var(--rule-hairline);
  }

  .row {
    display: grid;
    gap: 0.2rem 2rem;
    padding-block: 0.9rem;
    border-block-end: 1px solid var(--rule-hairline);
  }

  .row__key {
    font-size: var(--text-meta);
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--text-muted);
    /* Dates and figures are data, not prose. */
    font-variant-numeric: tabular-nums slashed-zero;
  }

  .row__value {
    max-inline-size: var(--container-measure);
  }

  @media (min-width: 40rem) {
    .row {
      grid-template-columns: 12rem minmax(0, 1fr);
      align-items: baseline;
    }
  }

  .sources {
    margin-block-start: 1.75rem;
  }

  .sources__label,
  .rule-note {
    font-size: var(--text-meta);
  }

  .sources__label {
    font-weight: 700;
    letter-spacing: var(--text-label--letter-spacing);
    text-transform: uppercase;
    color: var(--text-strong);
  }

  .sources ul {
    list-style: none;
    padding: 0;
    font-size: var(--text-meta);
    color: var(--text-muted);
  }

  .sources li + li {
    margin-block-start: 0.6rem;
  }

  .sources__date {
    margin-block-start: 1rem;
    font-size: var(--text-meta);
    color: var(--text-muted);
  }

  .sources__stale {
    display: block;
    font-weight: 700;
    color: var(--text-strong);
  }

  blockquote {
    margin: 0;
    padding-inline-start: 1rem;
    border-inline-start: 3px solid var(--accent-spine);
  }

  cite {
    display: block;
    margin-block-start: 0.5rem;
    font-size: var(--text-meta);
    font-style: normal;
    color: var(--text-muted);
  }

  .myth__claim {
    color: var(--text-muted);
  }

  .myth__why {
    margin-block-start: 0.5em;
  }

  .rule-note {
    margin-block-start: 1.75rem;
    padding-block-start: 1.25rem;
    border-block-start: 3px solid var(--accent-dot);
  }

  /* Links inherit their colour and are underlined with the hairline, matching
     SiteFooter — teal-bright and orange never carry text in the light theme. */
  a {
    text-decoration-color: currentColor;
    text-underline-offset: 3px;
  }
</style>
